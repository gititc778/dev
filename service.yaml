apiVersion: v1
kind: Service
metadata:
  name: my-service
spec:
  type: NodePort
  selector:
    name: app1
  ports:
    - name: http
      port: 8080
      # targetPort: 9000
      nodePort: 30080
    # - name: metrics
    #   port: 8080
    #   targetPort: 8080
    #   nodePort: 30081